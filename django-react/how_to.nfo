*** INSTALLATION ***

0. CMD -> pip install pipenv
1. CMD -> Folder django-react
2. pipenv --python 3.6
* if needed: pipenv shell
3. pipenv install
4. run ***migrate.bat***
5. cd ../frontend
*. npm install -g create-react-app
6. npm install
7. npm start 

if everything works, open ***run.bat***